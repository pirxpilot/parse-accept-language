var should = require('should');
var parseAcceptLanguage = require('../');

describe('parse-accept-language node module', function () {
  it('must have at least one test', function () {
    parseAcceptLanguage();
    should.fail('Need to write tests.');
  });
});
